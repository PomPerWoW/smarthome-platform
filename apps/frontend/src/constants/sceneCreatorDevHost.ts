/**
 * LAN IP for scene-creator (`npm run dev:network`). Kept in sync by `npm run set-ip <ip>`.
 * Enter 3D World uses https://<this>:3003/smarthome/xr/ (overridable via VITE_HOST_IP / VITE_HOST_SCENE_CREATOR_URL).
 */
export const SCENE_CREATOR_DEV_NETWORK_HOST = "192.168.4.181";
