/**
 * Shared visual scale for humanoid RPM-style avatars (player + NPCs).
 * Collision radii/heights in collision.ts are scaled relative to this baseline.
 */
export const AVATAR_VISUAL_SCALE = 0.8;
