name: Scene Creator CI

on:
  push:
    branches: [main]
    paths:
      - "apps/scene-creator/**"
      - ".github/workflows/scene-creator-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "apps/scene-creator/**"
      - ".github/workflows/scene-creator-ci.yml"

jobs:
  test:
    name: Run Scene Creator Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/scene-creator

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Run tests
        run: npm run test

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Verify build
        run: npm run build
